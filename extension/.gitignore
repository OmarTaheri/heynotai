node_modules
.output
.wxt
stats.html
stats-*.json
*.log
web-ext.config.ts

# Private key that pins the dev extension ID — anyone with this can
# publish updates that look like they came from us. Never commit.
*.pem
